{
    "name": "bootstrap",
    "main": "build/main.js",
    "type": "module",
    "private": true,
    "scripts": {
        "build": "tsc",
        "pulumi:up": "pulumi up -y --refresh -s ${PULUMI_STACK_NAME:?} --skip-preview",
        "pulumi:stack:select": "pulumi stack select -s ${PULUMI_STACK_NAME:?} --create"
    },
    "dependencies": {
        "@pulumi/pulumi": "^3",
        "@pulumi/kubernetes": "^4"
    }
}