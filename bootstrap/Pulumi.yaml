name: bootstrap
runtime: 
  name: nodejs
  options:
    typescript: false
description: Bootstraps SDP on Kubernetes Cluster
config:
  manifest:
    type: string
    description: Path to the bootstrap manifest
  pulumi:disable-default-providers:
    - '*'
